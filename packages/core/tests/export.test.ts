import { describe, it } from 'vitest'

// TODO: import { UseLimitClient } from '../src/index.js'
// TODO: import { InMemoryAdapter } from '../src/storage/memory.js'

describe('exportUsage()', () => {
  it.todo('returns all events for a tenant when no filters are applied')
  it.todo('filters events by userId')
  it.todo('filters events by feature')
  it.todo('filters events by from/to date range')
  it.todo('returns correct count in ExportResult')
  it.todo('serializes events as valid JSON when format=json')
  it.todo('serializes events as valid CSV when format=csv')
  it.todo('CSV output includes a header row with correct column names')
  it.todo('returns an empty result set when no events match the filter')
})
